<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <!-- 白い熊 保存復元: an automation run names its own backup directory, so the export must be
         able to write an arbitrary absolute path. Granted by hand in system settings. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- 白い熊 保存復元 v2: the data door's export/import service. Not dataSync — that type is
         metered and interruptible on API 34+, and this is a one-shot job another app is blocked on. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

<!--    <permission android:name="ac.mdiq.podcini.BIND_PROVIDER" android:protectionLevel="normal" />-->

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true"/>

    <uses-feature
        android:name="android.hardware.screen.portrait"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false"/>

    <!--        android:usesCleartextTraffic="true"-->

    <application
        android:name="ac.mdiq.podcini.PodciniApp"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:backupAgent=".config.OpmlBackupAgent"
        android:restoreAnyVersion="true"
        android:theme="@style/Theme.Podcini.Splash"
        android:supportsRtl="true"
        android:logo="@mipmap/ic_launcher"
        android:resizeableActivity="true"
        android:allowAudioPlaybackCapture="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33">

        <service android:name=".playback.service.PlaybackService"
                 android:foregroundServiceType="mediaPlayback"
                 android:label="@string/app_name"
                 android:enabled="true"
                 android:exported="true"
                 tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService"/>
                <action android:name="android.media.browse.MediaBrowserService"/>
                <action android:name="ac.mdiq.podcini.intents.PLAYBACK_SERVICE" />
            </intent-filter>
        </service>

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:enabled="true"
            tools:replace="android:enabled,android:foregroundServiceType" />

        <receiver
            android:name=".receiver.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
            <intent-filter>
                <action android:name="ac.mdiq.podcini.NOTIFY_BUTTON_RECEIVER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.TimerReceiver"
            android:enabled="true"
            android:exported="false">
        </receiver>

        <service
            android:name=".playback.service.QuickSettingsTileService"
            android:enabled="true"
            android:exported="true"
            android:label="@string/app_name"
            android:icon="@drawable/ic_notification"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.ACTIVE_TILE" android:value="true" />
            <meta-data android:name="android.service.quicksettings.TOGGLEABLE_TILE" android:value="true" />
        </service>

        <meta-data android:name="android.webkit.WebView.MetricsOptOut"
            android:value="true"/>
        <meta-data android:name="com.google.android.gms.car.notification.SmallIcon"
            android:resource="@drawable/ic_notification"/>
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc"/>
        <meta-data
            android:name="com.google.android.backup.api_key"
            android:value="AEdPqrEAAAAI3a05VToCTlqBymJrbFGaKQMvF-bBAuLsOdavBA"/>
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="ac.mdiq.podcini.playback.cast.CastOptionsProvider" />

        <!-- Version < 3.0. DeX Mode and Screen Mirroring support -->
        <meta-data android:name="com.samsung.android.keepalive.density" android:value="true"/>
        <!-- Version >= 3.0. DeX Dual Mode support -->
        <meta-data android:name="com.samsung.android.multidisplay.keep_process_alive" android:value="true"/>

        <activity
            android:name=".activity.MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout|density|uiMode|keyboard|navigation"
            android:windowSoftInputMode="stateAlwaysHidden"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <action android:name="android.intent.action.MUSIC_PLAYER" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.APP_MUSIC" />
                <category android:name="com.android.launcher3.DEEP_SHORTCUT" />

                <action android:name="ac.mdiq.podcini.intents.MAIN_ACTIVITY" />
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES"/>
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".activity.ShareReceiverActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.Podcini"
            android:label="@string/add_feed_label"
            android:exported="true"
            android:parentActivityName="ac.mdiq.podcini.activity.MainActivity">

            <!-- Files with mimeType rss/xml/atom -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="http"/>
                <data android:host="*"/>
                <data android:scheme="https"/>
                <data android:mimeType="text/xml"/>
                <data android:mimeType="application/rss+xml"/>
                <data android:mimeType="application/atom+xml"/>
                <data android:mimeType="application/xml"/>
            </intent-filter>

            <!-- Podcast protocols -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="itpc"/>
                <data android:scheme="pcast"/>
                <data android:scheme="feed"/>
                <data android:scheme="podcini-subscribe"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.BugReportActivity"
            android:theme="@style/Theme.Podcini">
        </activity>

        <activity
            android:name=".activity.QueuePickerActivity"
            android:theme="@style/Theme.Podcini.Dialog"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:exported="false" />

        <activity
            android:name=".activity.EpisodeInfoActivity"
            android:theme="@style/Theme.Podcini.Dialog"
            android:launchMode="singleInstance"
            android:taskAffinity=".episode_info"
            android:excludeFromRecents="true"
            android:exported="false" />

        <activity
            android:name=".activity.PlayerUIActivity"
            android:theme="@style/Theme.Podcini.Dialog"
            android:launchMode="singleInstance"
            android:taskAffinity=".player_ui"
            android:excludeFromRecents="true"
            android:exported="false" />

        <activity
            android:name=".activity.ShortcutsActivity"
            android:exported="false"
            android:theme="@style/Theme.Podcini">
        </activity>

        <activity-alias
            android:name=".FeedAlias"
            android:targetActivity=".activity.ShortcutsActivity"
            android:label="@string/podcast"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".QueueAlias"
            android:targetActivity=".activity.ShortcutsActivity"
            android:label="@string/queue"
            android:exported="true"
            android:parentActivityName="ac.mdiq.podcini.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".FacetAlias"
            android:targetActivity=".activity.ShortcutsActivity"
            android:label="@string/facet"
            android:exported="true"
            android:parentActivityName="ac.mdiq.podcini.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
            </intent-filter>
        </activity-alias>

        <receiver
            android:name=".receiver.WidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- 白い熊 保存復元: the headless state export, and the cancel that stops one. No
             android:permission — since contract v2 this is the deliberately unauthenticated half of
             the surface: it only ever writes where it was told to and reports what it did.
             CANCEL_EXPORT must be reachable from outside, so it is routed through this exported
             receiver rather than the (correctly unexported) service below. -->
        <receiver
            android:name=".config.automation.StateExportReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="${applicationId}.action.EXPORT_STATE" />
                <action android:name="${applicationId}.action.LIST_CATEGORIES" />
                <action android:name="${applicationId}.action.CANCEL_EXPORT" />
            </intent-filter>
        </receiver>

        <!-- The data door (contract v2 §2a): everything that moves bytes through a caller-supplied
             descriptor, and the only place `import` exists. No android:permission by design — a
             custom one would have to be `normal` (any app may hold it) or `signature` (impossible:
             every app in this family has its own keystore). AutomationCallers checks the caller by
             exact package name, uid and pinned signing certificate instead. -->
        <provider
            android:name=".config.automation.AutomationProvider"
            android:authorities="${applicationId}.automation"
            android:exported="true"
            tools:ignore="ExportedContentProvider" />

        <service
            android:name=".config.automation.AutomationDataService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Automation data export/import" />
        </service>

        <!-- Where the §1 EXPORT_STATE export runs. It cannot run in the receiver: goAsync() does
             NOT extend the broadcast window (~10 s foreground, ~60 s otherwise), and this app's
             archive carries a realm snapshot that takes minutes on a real library — overrunning it
             ANRs the process and kills it mid-export. dataSync rather than specialUse because that
             is exactly what this is, and it needs no subtype property. -->
        <service
            android:name=".config.automation.StateExportService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Capability discovery, read straight off the manifest with MATCH_UNINSTALLED_PACKAGES |
             MATCH_DISABLED_COMPONENTS. Not a query intent: 白い熊 freezes apps aggressively and a
             frozen app cannot be asked anything, yet 応用管理 must still answer "can this app be
             backed up". An app without these entries is simply never offered for data backup. -->
        <meta-data android:name="shiroikuma.automation.contract"   android:value="2" />
        <meta-data android:name="shiroikuma.automation.format"     android:value="1" />
        <meta-data android:name="shiroikuma.automation.min_format" android:value="1" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <meta-data
            tools:ignore="Deprecated"
            android:name="com.google.android.actions"
            android:resource="@xml/actions" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <queries>
        <intent>
            <action android:name="ac.mdiq.podcini.action.PODCINI_GATEWAY" />
        </intent>
    </queries>

    <!-- 白い熊 保存復元: without this our reply broadcast's setPackage() fails SILENTLY on
         Android 11+ — the export runs, writes correctly, and is never heard of. -->
    <queries>
        <package android:name="shiroikuma.oyokanri" />
        <package android:name="shiroikuma.jiyusagyoban" />
    </queries>

</manifest>
